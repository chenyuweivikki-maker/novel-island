# Novel Island - 小说岛：创作者的第二大脑

> AI Agent平台，为中长篇网文创作者提供知识管理、一致性校验、灵感拓展和情感陪伴

## 项目状态：8个里程碑全部完成 ✅

基于 LangGraph 状态机的完整 Agent 系统，覆盖建库、问答、图谱、成本控制全链路。

## 项目结构

novel-island/
├── backend/
│   ├── app/
│   │   ├── core/           # 核心：配置/分块/检索/LLM客户端/记忆/模型路由/图谱
│   │   ├── nodes/          # LangGraph节点（qa_nodes/build_nodes）
│   │   ├── graphs/         # LangGraph状态机（qa_graph/build_graph）
│   │   ├── tools/          # 工具集（search_kb）
│   │   ├── models/         # 状态定义（NovelIslandState）
│   │   └── main.py         # FastAPI入口
│   ├── verify_memory.py    # 多轮记忆验证脚本
│   └── requirements.txt    # 依赖清单
├── frontend/public/        # 前端原型
├── config/                 # 配置
├── data/                   # 数据（示例/图谱持久化）
└── docs/                   # 文档

## 技术栈

| 层 | 技术 | 职责 |
|----|------|------|
| 编排层 | LangGraph | 状态机、节点编排、条件路由、并行 |
| API | FastAPI | 后端接口 |
| LLM | DeepSeek | 问答/抽取/质检/摘要 |
| 检索 | TF-IDF (scikit-learn) | 知识库检索 |
| 存储 | 内存 + JSON | 图谱持久化 |

## 8个里程碑实现的能力

| # | 能力 | 核心概念 |
|---|------|---------|
| 1 | 问答链路状态机 | State / Node / Graph |
| 2 | 意图路由 | conditional edge（条件分支） |
| 3 | 工具调用 | tool calling（LLM自主决定调工具） |
| 4 | 防幻觉质检 | LLM-as-Judge + 循环回边（打回重试） |
| 5 | 并行建库 | 并行分支 + 汇合（实体/事件/关系抽取） |
| 6 | 三层记忆 | 短期对话历史 + 摘要压缩 |
| 7 | 模型路由+成本 | 三级模型路由 + token成本记录 |
| 8 | 知识图谱 | 实体/关系图谱 + 多跳查询 + 持久化 |

## 快速开始

```bash
# 后端（同时托管前端，单端口）
cd backend
pip install -r requirements.txt
cp .env.example .env  # 填入 DeepSeek API Key
uvicorn app.main:app --reload
# 打开 http://localhost:8000

API 接口

┌───────────────────────────┬─────────────────────────────────────┐
│           接口            │                功能                 │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/kb/build        │ 构建知识库（状态机+并行抽取）       │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/kb/ask          │ 提问（意图路由→工具调用→质检→记忆） │
├───────────────────────────┼─────────────────────────────────────┤
│ GET /api/kb/status        │ 知识库状态                          │
├───────────────────────────┼─────────────────────────────────────┤
│ GET /api/graph            │ 图谱数据                            │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/graph/neighbors │ 实体邻居查询                        │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/graph/path      │ 实体路径查询（多跳推理）            │
├───────────────────────────┼─────────────────────────────────────┤
│ GET /api/timeline         │ 故事脉络（章节事件摘要）            │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/novel           │ 创建作品                            │
├───────────────────────────┼─────────────────────────────────────┤
│ GET /api/novels           │ 作品列表                            │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/chapter         │ 保存章节（冲突检测+章纲+增量入库）  │
├───────────────────────────┼─────────────────────────────────────┤
│ 灵感库 (P12)              │ POST/GET /api/inspirations、分类管理│
│                           │ （AI 自动分类：人设/剧情/金句/世界观）│
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/polish          │ 润色（编辑器 Review 弹窗用）        │
├───────────────────────────┼─────────────────────────────────────┤
│ POST /api/analysis/report │ 创作策略报告（DataAnalyst Agent）   │
├───────────────────────────┼─────────────────────────────────────┤
│ 社区 (作者互动)           │ POST /api/community/post 发帖        │
│                           │ GET /api/community/posts 帖子流      │
│                           │ 点赞/收藏/评论（toggle + comment）   │
│                           │ 灵感卡片一键分享到社区               │
├───────────────────────────┼─────────────────────────────────────┤
│ GET /api/cost             │ 成本记录汇总                        │
├───────────────────────────┼─────────────────────────────────────┤
│ GET /api/health           │ 健康检查                            │
└───────────────────────────┴─────────────────────────────────────┘

前端能力（对齐 UI 定稿 v0.2 全量页面）
- 首页（最近作品）/ 我的作品 / 创作工作台 8 tab / 登录注册 UI
- 灵感库：作者上传 → AI 自动分类 → 分类浏览/管理/导出
- 人物关系图：力导向（拖拽/缩放/点击弹人设卡）
- 写作编辑器三栏：章节列表 + 编辑区 + AI 对话联动；选中文本润色 Review（原文 vs 修改）
- 卡文检测：编辑停顿超 5 分钟弹窗求助（自动带入前文推演）
- 创作策略报告：DataAnalyst 分析章节/图谱/大纲 → 结构化报告

测试

# 多轮记忆验证
cd backend && ./.venv/bin/python verify_memory.py
